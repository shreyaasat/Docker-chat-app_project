name: CI
on:
 push:
   branches: [ master ]
 pull_request:
   branches: [ master ]
 workflow_dispatch:
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
      - uses: actions/checkout@v2
      
      - name: Scripts
        run:
         docker build -t app .
         docker login -u shreyaasatish -p dckr_pat_CNgIDnjbxjCMhq9c9l2FeYdB8vY
         docker tag app shreyaasatish/virtuproj
         docker push shreyaasatish/virtuproj
